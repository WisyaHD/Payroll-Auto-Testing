import Config from '../../../fixtures/config.json';

let token = Config['x-auth-token'];
let portalBackemd = Config['server-ip']['qc-nagatech'];

describe('GettingDataWithRESTAPI', () => {
    it('running with backend', () => {

    })
})